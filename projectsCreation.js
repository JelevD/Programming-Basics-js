function projectCreation(input) {
    let arhitect = input[0];
    let project = input[1];
    let totalhours = project * 3;
    console.log(`The architect ${arhitect} will need ${totalhours} hours to complete ${project} project/s.`)
}
projectCreation(["Sanya",
"9"])
